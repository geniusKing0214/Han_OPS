import type { EventItem } from "@/types/schedule";

export type { Slot, Session, EventItem } from "@/types/schedule";

/** 빈 배열 — 일정은 Firestore Admin에서 등록 */
export const mockEvents: EventItem[] = [];
