import { AdminNoticesPanel } from "@/components/admin/admin-notices-panel";

export const metadata = { title: "알림 발송 — HAN OPS" };

export default function AdminNoticesPage() {
  return <AdminNoticesPanel />;
}
